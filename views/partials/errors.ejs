<% if (locals.errors) { %>
    <ul>
        <% errors.forEach(function(error) { %>
            <li class="error"><%= error.msg %></li>
       <% }); %>
    </ul>
<% } else { %>
    <p class="placeholder">error</p>
<% } %>